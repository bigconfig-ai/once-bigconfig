BigConfig now supports Clojure, TypeScript, and Python 🚀

The first agentic package manager — agents maintain every implementation, and the rendered config files are the test suite.

Define once. Run everywhere.

bigconfig.ai
