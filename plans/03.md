# Goal
- Make a slide with svg for a marketing campaign for BigConfig
- Make the linkedin post

# Requirement
- to install the package bigconfig-ai/once you need a one-line prompt
- the prompt is "run npx bc-pkg bigconfig-ai/once validate and iterate on failures"
- model used deepseek-v4-flash
- total cost 1.3 cents

# Deliverables
- Slide: `public/campaign-cost.svg` (1200x627, LinkedIn share format, matches site brand palette)
- LinkedIn post copy: see below

# LinkedIn post

We just installed BigConfig on a fresh project.

Total cost: $0.013.

Not a typo. One cent and three tenths — less than the rounding error on your cloud bill.

No setup wizard. No 40-page docs. No "clone this, export that, now configure the YAML." You paste a single line into your AI coding agent:

run npx bc-pkg bigconfig-ai/once validate and iterate on failures

…and it installs itself. It validates, hits failures, fixes them, and re-runs until it's green. The whole loop ran on deepseek-v4-flash and cost just over a penny.

This is what "agentic infrastructure" actually means: you describe the outcome, the agent owns the toil. Onboarding goes from an afternoon to the time it takes to read this post.

BigConfig — the first agentic infrastructure package manager.

👉 bigconfig.ai

#DevOps #Infrastructure #AI #Clojure #DeveloperExperience #Automation
