# Goal
Make a slide with svg for a marketing campaign for BigConfig

# Requirement
- to install the package bigconfig-ai/once you need a one-line prompt
- the prompt is "run BB_EDN_REPO=bigconfig-ai/once npx @bigconfig/bb validate and iterate on failures"
