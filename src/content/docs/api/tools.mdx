---
title: tools
description: BigConfig Render
sidebar:
  order: 5
---
# <a name="big-config.tools">big-config.tools</a>


These are the templates available in BigConfig out of the box.

  ### Available Templates

    * [`package`](#big-config.tools/package): Scaffold a BigConfig package for deep automation.
    * [`devenv`](#big-config.tools/devenv): Generate [`devenv`](#big-config.tools/devenv) files for Clojure and Babashka development.
    * [`action`](#big-config.tools/action): Create GitHub Actions workflows for Clojure projects.

  -----

  ### DEPRECATED Templates

  > **Note:** These templates must be migrated from `step` to `workflow`.

    * [`terraform`](#big-config.tools/terraform): Scaffold a project to manage Terraform/OpenTofu with BigConfig.
    * [`dotfiles`](#big-config.tools/dotfiles): Manage user configuration (dotfiles) with templates.
    * [`ansible`](#big-config.tools/ansible): Scaffold an Ansible-based infrastructure project.
    * [`multi`](#big-config.tools/multi): A hybrid template for projects using both Ansible and Terraform.
    * [`tools`](#big-config.tools/tools): Create a `tools.clj` for programmatic workflow control.
    * [`generic`](#big-config.tools/generic): A base template for generic infrastructure projects.

  See [Clojure Tools](/guides/clojure-tools/) guide.
  




## <a name="big-config.tools/action">`action`</a>
``` clojure
(action & {:as args})
```
Function.

Create a GitHub action for the CI of a Clojure project.

  Options:
  - :target-dir  target directory for the template ([`action`](#big-config.tools/action) is the default)
  - :overwrite   true or :delete (the target directory)

  Example:
    clojure -Tbig-config action
<p><sub><a href="https://github.com/bigconfig-ai/big-config/blob/main/src/clj/big_config/tools.clj#L305-L321">Source</a></sub></p>

## <a name="big-config.tools/ansible">`ansible`</a>
``` clojure
(ansible & {:as args})
```
Function.

DEPRECATED, use the [`package`](#big-config.tools/package) template

  Create a repo to manage Ansible projects with BigConfig.

  Options:
  - :target-dir  target directory for the template ([`ansible`](#big-config.tools/ansible) is the default)
  - :overwrite   true or :delete (the target directory)

  Example:
    clojure -Tbig-config ansible
<p><sub><a href="https://github.com/bigconfig-ai/big-config/blob/main/src/clj/big_config/tools.clj#L255-L273">Source</a></sub></p>

## <a name="big-config.tools/data-fn">`data-fn`</a>
``` clojure
(data-fn {:keys [owner repository], :as data} _ops)
```
Function.
<p><sub><a href="https://github.com/bigconfig-ai/big-config/blob/main/src/clj/big_config/tools.clj#L118-L124">Source</a></sub></p>

## <a name="big-config.tools/devenv">`devenv`</a>
``` clojure
(devenv & {:as args})
```
Function.

Create the devenv files for Clojure and Babashka development.

  Options:
  - :target-dir  target directory for the template (current directory is the default)

  Example:
    clojure -Tbig-config devenv
<p><sub><a href="https://github.com/bigconfig-ai/big-config/blob/main/src/clj/big_config/tools.clj#L209-L224">Source</a></sub></p>

## <a name="big-config.tools/dotfiles">`dotfiles`</a>
``` clojure
(dotfiles & {:as args})
```
Function.

DEPRECATED: clone https://github.com/amiorin/dotfiles-v3

  Create a repo to manage dotfiles with BigConfig.

  Options:
  - :target-dir  target directory for the template ([`dotfiles`](#big-config.tools/dotfiles) is the default)
  - :overwrite   true or :delete (the target directory)

  Example:
    clojure -Tbig-config dotfiles
<p><sub><a href="https://github.com/bigconfig-ai/big-config/blob/main/src/clj/big_config/tools.clj#L231-L248">Source</a></sub></p>

## <a name="big-config.tools/generic">`generic`</a>
``` clojure
(generic & {:as args})
```
Function.

DEPRECATED, use the [`package`](#big-config.tools/package) template

  Create a repo to manage a generic projects with BigConfig.

  Options:
  - :target-dir  target directory for the template ([`generic`](#big-config.tools/generic) is the default)
  - :overwrite   true or :delete (the target directory)

  Example:
    clojure -Tbig-config generic
<p><sub><a href="https://github.com/bigconfig-ai/big-config/blob/main/src/clj/big_config/tools.clj#L363-L382">Source</a></sub></p>

## <a name="big-config.tools/multi">`multi`</a>
``` clojure
(multi & {:as args})
```
Function.

DEPRECATED, use the [`package`](#big-config.tools/package) template

  Create a repo to manage both Ansible and Terraform projects with BigConfig.

  Options:
  - :target-dir  target directory for the template ([`multi`](#big-config.tools/multi) is the default)
  - :overwrite   true or :delete (the target directory)

  Example:
    clojure -Tbig-config multi
<p><sub><a href="https://github.com/bigconfig-ai/big-config/blob/main/src/clj/big_config/tools.clj#L280-L298">Source</a></sub></p>

## <a name="big-config.tools/package">`package`</a>
``` clojure
(package & {:as args})
```
Function.

Create a BigConfig package.

  Options:
  - :owner       GitHub owner
  - :repository  GitHub repository
  - :ssh-key     Digitalocean ssh-key
  - :target-dir  target directory for the template ([`package`](#big-config.tools/package) is the default)
  - :overwrite   true or :delete (the target directory)

  Example:
    clojure -Tbig-config package
<p><sub><a href="https://github.com/bigconfig-ai/big-config/blob/main/src/clj/big_config/tools.clj#L126-L159">Source</a></sub></p>

## <a name="big-config.tools/step-fns">`step-fns`</a>



<p><sub><a href="https://github.com/bigconfig-ai/big-config/blob/main/src/clj/big_config/tools.clj#L97-L99">Source</a></sub></p>

## <a name="big-config.tools/terraform">`terraform`</a>
``` clojure
(terraform & {:as args})
```
Function.

DEPRECATED, use the [`package`](#big-config.tools/package) template

  Create a repo to manage Terraform/Tofu projects with BigConfig.

  Options:
  - :target-dir  target directory for the template (`terrafom` is the default)
  - :overwrite   true or :delete (the target directory)
  - :aws-profile aws profile in ~/.aws/crendentials
  - :region      aws region
  - :dev         aws account id for dev
  - :prod        aws account id for prod

  Example:
    clojure -Tbig-config terraform :region us-west-1
<p><sub><a href="https://github.com/bigconfig-ai/big-config/blob/main/src/clj/big_config/tools.clj#L172-L198">Source</a></sub></p>

## <a name="big-config.tools/tools">`tools`</a>
``` clojure
(tools & {:as args})
```
Function.

DEPRECATED, use https://github.com/bigconfig-ai/big-config/blob/main/src/clj/big_config/tools.clj

  Create a tools.clj for a Clojure project.

  Options:
  - :target-dir  target directory for the template (current directory is the default)
  - :path        path for the clojure source code
  - :ns          namespace containing the file tools.clj
  - :name        override the default name [`tools`](#big-config.tools/tools)

  Example:
    clojure -Tbig-config tools
<p><sub><a href="https://github.com/bigconfig-ai/big-config/blob/main/src/clj/big_config/tools.clj#L331-L353">Source</a></sub></p>

